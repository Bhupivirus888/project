1. ssh-keygen -t rsa -b 4096 -C "bhupendra.singh@ebizondigital.com"

2. ls -l ~/.ssh/
3. cat ~/.ssh/id_rsa.pub
 then copy key and paste in github acc into setting -> addd new key 
 
 then come back to teminal 
 
4. git init 
the add somefile 
5.  git status
6. fir add .
7. git commit -m "MSG"
8. git config --global user.name "bhupendrasingh888"
9. git config --global user.email "bhupendra.singh@ebizondigital.com"

  #####after first commit #######
10.  git remote set-url origin git@github.com:Bhupivirus888/project.git
11. git push origin main



<<<<<<<<<< From Local to staging  Push code <<<<<<<<<<<<<<<<
1. Go To html/bmi/bmi-beta open in vscode 
2.open new terminal and run commands
       git config --global user.name "bhupendrasingh888"
       git config --global user.email "bhupendra.singh@ebizondigital.com"
       git status

then run commands  ###########################################################
       git add FILE_NAME
       git status
       git commit -m "MSG"
       git pull origin staging
       git push origin BRANCH-NAME
  then goto server from terminal and take pull
       git pull origin staging

Merge for production: ############################################################
  (vscode)  git checkout production
      	    git merge staging
            git pull origin production
            git push origin production
    then goto server from terminal and take pull
            git pull origin production
       
       

SOME commands 
git pull origin master
git checkout -b BRANCH-NAME
git branch -a
git pull origin BRANCH-NAME
git status
git add ALL-FILES-THOSE-ARE-IN-RED
git commit -m "MSG"
git push -f origin BRANCH-NAME

